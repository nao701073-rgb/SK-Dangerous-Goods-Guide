# SKDG v1.3.31 利用者名表示・ユーザー設定導線

- 右上の固定文言「利用者」を、現在ログインしている利用者の `displayName`（なければ name / loginId）へ置換します。
- 危険物検索・危険物詳細は既存の右上ピルをそのまま利用します。
- `app-header` を使用する主要機能には同じ意匠の利用者ピルを右側へ追加します。
- 丸アイコンは利用者名の先頭1文字を表示します。
- ピルをクリック、またはキーボードで Enter / Space するとユーザー設定へ移動します。
- v1.3.30の権限メニュー制御は変更しません。
