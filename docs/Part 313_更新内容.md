# Part 313 更新内容

## 無操作時の自動ログアウト強化

- ユーザー設定から自動ログアウト時間を15分、30分、60分、120分から選択可能にした。
- 初期値は30分とし、終了2分前に継続確認ダイアログを表示する。
- 「ログインを継続」でタイマーを更新し、「今すぐログアウト」で直ちにログアウトできる。
- 複数タブ間で操作時刻と設定変更を共有する。
- 非表示タブから戻った際も経過時間を再判定する。
- 自動ログアウト後はログイン画面に理由を表示する。
- ログイン成功時のSK演出、ログイン後のテーマ、既存認証機能は維持する。
