# Part 388 更新内容

- 改正検証プレビュー遷移時にログイン画面へ戻る問題を修正。
- 全内部画面の認証初期化ファイルを Part 388 へ統一。
- 画面遷移時に認証トークン、利用者情報、最終操作時刻を共通処理で同期。
- 新規追加画面でも `auth-bootstrap.js` を読み込むだけで同じ認証維持仕様を適用。
- セッション監視が認証復元より先に動いても、遷移直後に誤ログアウトしない保護を追加。
- 権限不足の場合はログアウトではなくアクセス拒否画面を表示。
