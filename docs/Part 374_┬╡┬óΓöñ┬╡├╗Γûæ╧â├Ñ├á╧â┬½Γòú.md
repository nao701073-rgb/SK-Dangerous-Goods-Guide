# Part 374 更新内容

- 管理者を含む全権限で、ログイン後のページ移動時に認証状態を維持するよう修正。
- `file://` で直接起動した場合も、同一タブ内では `window.name` の認証ブリッジからログイン情報を復元。
- システム設定、ユーザー設定、各管理画面、一般機能間の移動でログイン画面へ戻る事象を修正。
- ログアウトおよび自動ログアウト時は認証ブリッジも確実に削除。
- URL直接アクセス時はログイン状態と権限を再確認し、未認証のみログイン画面へ移動。
