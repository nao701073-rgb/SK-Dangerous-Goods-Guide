# Part 278 更新内容

## 危険物検索の0件表示改善

- 検索結果が0件の場合に、入力内容または絞り込み条件の確認を案内します。
- 詳細条件が指定されている場合は「詳細条件だけ解除」を表示します。
- 「すべての条件を解除」から初期状態へ戻せます。
- 検索結果件数と0件案内に `aria-live` を設定し、読み上げ環境で結果更新を認識しやすくしました。
- スマートフォンでは解除ボタンを縦並びで表示します。
