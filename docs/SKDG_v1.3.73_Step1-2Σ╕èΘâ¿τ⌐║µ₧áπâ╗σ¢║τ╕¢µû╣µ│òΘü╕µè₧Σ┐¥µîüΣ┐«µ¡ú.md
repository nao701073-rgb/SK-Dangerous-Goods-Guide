# SKDG v1.3.73

## 修正内容
- Step 1 / Step 2 の見出しだけが `h2` であるため残っていたブラウザ標準 margin を `margin: 0` で明示的に解除。
- ①・②カードの外枠直下から見出し背景が始まるようにし、上部の空白帯／空枠に見える表示を解消。
- Step 3〜6 と同じ見出し接続状態へ統一。
- 固縛方法は「直接固縛」「トップオーバー」の2択を維持。
- トップオーバー選択後に一時的に「支保・あて材のみ」へ切り替え、再び引張系固縛材へ戻した場合も、直前の固縛方法を復元。

## 非変更項目
- 固縛力算出式、方向別計算、MSL/STF計算ロジックは変更していない。
- ①〜⑥の順序、追跡表示、申請番号管理登録処理は変更していない。
