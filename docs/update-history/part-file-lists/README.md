# PART UPDATE FILE LIST 保管フォルダ

`PARTxxx_UPDATE_FILE_LIST.json` は開発・更新履歴用のファイルです。
今後はリポジトリのルート直下ではなく、このフォルダにまとめて保管します。
システム本体の実行ファイルから直接参照する用途ではありません。

既存リポジトリの整理は `ORGANIZE_UPDATE_HISTORY.cmd` を実行してください。
旧 `ORGANIZE_PART_FILE_LISTS.cmd` も互換用として同じ整理処理を呼び出します。
