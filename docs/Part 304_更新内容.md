# Part 304 更新内容

## 関連資料の確認期限管理

- 現在表示中の資料へ、同一の確認期限をまとめて設定できるようにしました。
- 現在表示中の資料から、確認期限をまとめて解除できるようにしました。
- 一括操作は検索・分類・言語・確認状態などで絞り込んだ後の表示対象だけに適用されます。
- 操作前に対象件数と設定内容を表示し、確認後に反映します。
- 確認期限の絞り込みに「期限未設定」を追加しました。
- 集計カードに「期限未設定」の件数を追加しました。

既存の確認手続き、個別期限、お気に入り、要確認、メモ、閲覧履歴、個人設定JSONとの互換性を維持しています。
