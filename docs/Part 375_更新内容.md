# Part 375 更新内容

- 管理者を含む全権限で、ログイン後にユーザー設定・システム設定・各機能画面へ移動した際の認証状態を維持。
- `file://` 直接起動時のブラウザー差異に対応するため、同一タブの認証ブリッジに加えて一時的なURLフラグメントで認証情報を受け渡し、遷移後にURLから自動除去。
- 権限不足の場合はログイン画面ではなく権限不足画面を表示する既存仕様を維持。
- ログアウト時および自動ログアウト時は認証情報を削除。
- ホーム画面の検索キーワード例から「消防法」を削除し、「法令名」へ変更。
