# Part 167 スマートフォン実寸比較の横画面対応

## 対応内容

- `screen.orientation.lock("landscape")` が利用できる端末では、従来どおり横向き固定を試行します。
- iPhone Safariなど方向固定に非対応の環境では、実寸比較パネルをCSSで90度回転し、画面内に横向き表示します。
- 横向き表示を終了できる「閉じる」ボタンを追加しました。
- 縦画面での通常表示では、正式輸送品名と国連番号を画面幅内で折り返します。
