# Part 279 更新内容

## 危険物検索

- 各検索結果カードに「お気に入り」と「国連番号をコピー」を追加しました。
- お気に入りは詳細画面を開かずに登録・解除できます。
- コピーする国連番号は `UN` と4桁番号を組み合わせた形式です。
- Clipboard APIを使用できない環境では従来方式へ切り替えます。
- 検索履歴へ保存する件数を、画面に表示中の件数ではなく条件に一致した総件数へ修正しました。

## 表示

- PCではカード下部に操作ボタンを横並びで表示します。
- 狭いスマートフォン画面では2列に分け、画面幅からはみ出さないようにしました。
