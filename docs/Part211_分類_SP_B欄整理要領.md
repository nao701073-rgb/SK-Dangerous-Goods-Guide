# Part 211 分類・SP・B欄整理要領

## 1. 整理方針

- 分類欄には `2.1`、`2.2`、`3`、`4.1`、`9` 等の分類値だけを保存する。
- `SP362`、`SP391` 等は `specialProvisions` に保存する。
- `BK1`、`BK2`、`BK3` 等は `flexibleBulkContainer` に保存し、危険物詳細画面の包装要件「B」欄に表示する。
- UN0190の分類値 `x` は変更しない。

## 2. 今回の処理

- 分類・SP分離対象：18件
- BK2移動対象：6件
- UN0190：変更なし、監査例外として明示
- 合計変更レコード：24件

## 3. 検証

`server` フォルダで次を実行する。

```powershell
npm run data:audit:strict
npm run data:release-check
```

Part 211作成時点の結果は、エラー0件、警告0件、公開判定「合格」である。

## 4. 更新時の注意

法令PDFやCSVから再抽出するときは、分類列、特別規定列、B列を別項目として取り込む。抽出結果に `2.2 SP362` や `BK2` がSP欄へ入った場合は、公開前に正規化と監査を行う。
