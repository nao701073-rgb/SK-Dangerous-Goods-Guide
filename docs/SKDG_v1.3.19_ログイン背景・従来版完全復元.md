# SKDG v1.3.19 ログイン背景・従来版完全復元

## 修正内容
- ユーザー指定の「以前のログイン画面（添付2）」と同じ港湾・コンテナターミナル背景へ戻した。
- 使用画像は既存の `assets/images/login-business-background-4k.png`（3840×2160）。
- v1.3.17〜v1.3.18で追加した朝・昼・夕方・夜の時間帯別背景切替はログイン画面から外した。
- `file://` で開いても背景画像が確実に見えるよう、背景はCSSの固定レイヤー（z-index 0）、ログインUIは前面（z-index 1）に固定した。
- 背景指定には `!important` を付け、旧スクリプトや後続CSSから時間帯画像へ差し替わらないようにした。
- SKロゴ、ログインフォーム、認証ロジック、表示文言は変更していない。
- DB移行なし。表示バージョンは `Version 1.0 試作版` のまま。
