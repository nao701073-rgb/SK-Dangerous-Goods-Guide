# Part 541 写真解析サービス接続

ブラウザは同一オリジンの `POST /api/ctu/photo-analysis` を呼び出します。
本更新ZIP単体には特定ベンダーの画像認識モデルやAPIキーを同梱しません。正式環境のサーバー側で、組織が承認した画像解析サービスへ接続してください。

入力・出力契約は `docs/part541_写真解析API契約.json` を使用します。認証、アクセス権、監査、写真保管方針は既存のサーバー側権限制御に従ってください。解析サービスが未接続・タイムアウト・エラーの場合、画面は手入力へフォールバックします。
