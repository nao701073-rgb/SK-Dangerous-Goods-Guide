const fs=require('fs'),path=require('path');const root=path.resolve(__dirname,'../..');const read=p=>fs.readFileSync(path.join(root,p),'utf8');const html=read('pages/ctu-securing-calculator.html'),css=read('assets/css/ctu-securing-part541.css'),version=JSON.parse(read('VERSION.json'));const checks={version:version.version==='part541',base:version.baseVersion==='part540',heading:html.includes('写真による固縛状態の確認補助'),cargoPoint:html.includes('貨物側取付点'),ctuPoint:html.includes('CTU側取付点'),lashingLine:html.includes('固縛材（2点で線）'),supportLine:html.includes('支保・当て材（2点で線）'),material:html.includes('ワイヤーロープ')&&html.includes('TY-GARD'),count:html.includes('securingVisualSummaryData'),apply:html.includes('applySecuringVisualToInputs'),noOcrPanel:!html.includes('写真の文字読取候補'),css:css.includes('securing-photo-assist'),report:html.includes('取付点・固縛状態写真台帳'),json:html.includes("release:'part541'")};const failed=Object.entries(checks).filter(([,v])=>!v);console.log(JSON.stringify({release:'part541',passed:Object.keys(checks).length-failed.length,total:Object.keys(checks).length,checks,failed:failed.map(([k])=>k)},null,2));process.exit(failed.length?1:0);